任务：
1、研究就是从整个识别结果中获取特定的数据
测试各个公司的不同



celery的流程
有几步，每一步是做什么，怎么做，
一般就是识别之后的数据库字段作为一个原始数据
比如整个识别结果，用来获取几个数据表的数据

整个识别结果，如何精准的获取到退保价值表，各个公司是不是一样的获取机制
比如像中银，他的数据表有的是 年龄 下面的数字是年龄而不是保单年度终结
每个步骤的实施逻辑在哪个文件，哪个函数

